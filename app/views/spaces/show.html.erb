<%= render "shared/header" %>
<body class = message-wrapper>
<div class="task-list" id="scroll_snap">
  <%= render "shared/error_messages", resource: @project %>
  <table class="table table-hover task">
    <tr>
      <th>タスク</th>
      <th>todo</th>
      <th>開始予定</th>
      <th>終了予定</th>
      <th>補足</th>
      <th>ステータス</th>
    </tr>
    <% @tasks.each do |task| %>
      <tr>
        <td rowspan= "<%= task.things.count %>" ><%= task.task_name %></td>
        <% task.things.each do |thing| %>
        <td><%= thing.thing_name %></td>
        <td><%= thing.start_time.strftime("%H:%M") %></td>
        <td><%= thing.end_time.strftime("%H:%M") %></td>
        <td><%= thing.memo%></td>
        <td><%= Status.find(thing.status_id).name %></td>
      </tr>
        <% end %>
     <% end %>
  </table>
  <%= link_to 'todo・ステータスを修正する', project_path(@project.id) %>
</div> 

<%# <div class="wrapper" id="scroll_snap">
  <div class="chat">
    <%= render "main_chat" %>
  <%# </div>
</div>  %>

<a class="pagetop" href="#">
  <div class="pagetop__arrow">
     <%= image_tag 'angle-up-solid.png'%>
  </div>
</a>
</body>